apiVersion: v1
kind: ConfigMap # Not a Secret - should trigger warning
metadata:
    name: not-a-secret
    namespace: default
data:
    config.yaml: |
        server:
          host: localhost
          port: 8080
    database.properties: |
        host=localhost
        port=5432
        database=myapp
